apiVersion: v2
name: employee-portal
description: Employee Portal — Cuba Corp internal web application
type: application
version: 0.1.0
appVersion: '0.1.0'

# IaC base — Inception skeleton
# Currently single-node Windows Server deployment (CON-008).
# This Helm chart is a placeholder for future containerized deployment
# if the architecture evolves. Current deployment target is IIS/Kestrel
# on Windows Server, not Kubernetes.
#
# Values template to be populated in Construction phase.
